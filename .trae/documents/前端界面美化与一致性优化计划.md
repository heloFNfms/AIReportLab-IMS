## 全局主题与样式系统
- 提取品牌色与渐变为 CSS 变量：在 `src/App.vue` 定义 `--brand-*`、`--card-radius`、`--text-*`，登录/注册/仪表盘统一引用
- 建立统一间距与阴影尺度：如 `--space-2/4/8/16`、`--shadow-sm/md/lg`，避免各页硬编码
- 支持暗色模式切换：在 `:root.dark` 下覆盖变量，提供暗色开关（保留 Element Plus 主题兼容）

## 布局与导航
- 顶部导航条统一样式：在 `Dashboard.vue:351-360` 使用变量驱动渐变背景、色值与阴影；登录/注册头部跟随风格
- 响应式布局优化：为 `el-row`/`el-col` 定义断点间距，保证手机端排版紧凑不溢出
- 未来侧边导航预留：如增加文件管理子区，统一入口按钮与标签

## 页面级优化
- 登录/注册页
  - 统一卡片圆角与阴影：登录 `Login.vue:125-132`、注册 `Register.vue:199-206` 改用 `--card-radius` 和 `--shadow-*`
  - 表单错误提示统一：Element Plus 校验后采用一致的错误样式与动效
  - 引入引导文案与次要链接样式一致化（如返回登录/注册）
- 仪表盘
  - 统计卡片视觉：`Dashboard.vue:389-398` 卡片圆角/悬浮阴影统一，图标色与字体大小对齐变量
  - 文件区
    - 空态与分页：`Dashboard.vue:94-152` 已加入空态与分页，完善加载骨架（`el-skeleton`）
    - 操作列按钮风格统一：微圆角与尺寸统一（通过 `:deep(.el-button)`）

## 组件与表格美化
- 表格细节：统一表头背景、行悬浮色、斑马纹强度、单元格内边距
- 标签与图标：使用 `:deep(.el-tag)` 圆角统一；图标颜色用变量控制，修复不可用图标（`CloudUpload` → `Cloudy`）
- 上传对话框：拖拽区风格统一、提示文案更友好，提交按钮 loading/禁用态一致

## 状态反馈与动效
- 全局消息：统一 `ElMessage` 成功/失败色值与停留时间，确保跨页一致体验
- 加载骨架：列表/统计卡片/表单提交时增加骨架或圆形加载，避免突兀空白
- 微动效：按钮 hover/active、卡片悬浮位移与阴影强度统一

## 响应式与可访问性
- 断点检查：手机/平板/桌面下的内边距、字号、卡片布局优化
- 可访问性：为主要交互控件添加 `aria-label` 与键盘可操作性；颜色对比度遵循 WCAG AA

## 代码质量与一致性
- 去除硬编码色值：用 CSS 变量替换散落的 `#409EFF/#909399` 等
- 深度选择器规范：`scoped` 下统一使用 `:deep(...)` 作用到 Element Plus 子组件
- 资源与图标统一：集中使用已注册的图标组件，避免字符串动态组件与未导出成员

## 交付与验证
- 分支提交方式：按模块拆分提交（主题系统→页面→组件→动效→响应式），每步附预览截图
- 本地验证：`http://localhost:5173/` 前端、`http://localhost:8000/` 后端；检查登录/注册/仪表盘在三端断点的表现

## 待实施的具体美化任务清单
- 建立 CSS 变量主题系统并替换硬编码色/圆角
- 为仪表盘统计卡片与文件区统一圆角、阴影与间距
- 为文件列表添加加载骨架、完善空态与分页视觉
- 统一表格表头/行样式与操作列按钮风格
- 修复并规范所有图标引用（含 OSS 标识图标）
- 登录/注册表单错误提示与说明文案一致化
- 添加暗色模式支持与主题切换入口
- 优化移动端排版与控件触摸区域
- 统一全局消息样式与动效参数

请确认以上计划，我将按上述任务逐项实现并在本地预览验证。